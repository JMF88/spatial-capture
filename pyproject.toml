[tool.pytest.ini_options]
testpaths = ["tests"]
markers = [
  "heavy: needs torch / GPU / model downloads (skipped in the light CI)",
]

[tool.ruff]
line-length = 100
target-version = "py312"
# docs/viewer/vendor is third-party code committed verbatim (see its README): linting
# someone else's build output would only ever produce noise we must not "fix".
extend-exclude = [".venv", "data", "runs", "out", "docs/viewer/vendor"]

[tool.ruff.lint]
# Real-error-focused: pyflakes (F) + a subset of pycodestyle + import sorting.
select = ["E", "F", "W", "I"]
# E501 (line too long) is noise for docstrings/tables; comments handle intent.
ignore = ["E501"]
